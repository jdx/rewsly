<h1><%= @story.title %></h1>
<% if @story.author %>
  <h2>Author: <%= link_to @story.author.name, author_path(@author) %></h2>
<% end %>
<%= @story.body %>

<p><%= link_to "Edit", edit_story_path(@story) %></p>